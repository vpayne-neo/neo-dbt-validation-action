name: 'Vince Payne'
description: 'Reads changed SQL and YML files anc checks for column equivalency'
author: 'Vince Payne - Neo Financial'

inputs:
  files:
    description: 'List of files to validate'
    required: true

runs:
  using: 'docker'
  image: 'node:18.15.0'
  entrypoint: 'dist/index.js'
