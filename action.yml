name: 'Vince Payne'
description: 'Reads changed SQL and YML files anc checks for column equivalency'
author: 'Vince Payne - Neo Financial'
inputs:
  milliseconds: # change this
    required: true
    description: 'input description here'
    default: 'default value if applicable'
  files:
    required: true
    description: 'list file pairs to be evaluated'
    default: null
runs:
  using: 'node16'
  main: 'dist/index.js'
